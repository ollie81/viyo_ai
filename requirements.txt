fastapi==0.111.0
uvicorn[standard]==0.30.1
openai==2.35.1

supabase==2.15.3
stripe==15.6.1
sentry-sdk[fastapi]==2.68.1
firebase-admin==7.5.0

PyJWT[cryptography]==2.10.1
cryptography>=43.0.0

python-dotenv==1.0.1
python-multipart==0.0.9
httpx==0.28.1
requests==2.32.3
pydantic==2.7.4

moviepy==1.0.3
ffmpeg-python==0.2.0

# Speaker-tracking crop (repurpose.py) — pinned to 4.10.0.84 rather than
# latest because opencv-python-headless 5.x stopped bundling the Haar
# cascade XML data files this feature depends on (cv2.data.haarcascades
# is an empty directory on 5.x); 4.10.0.84 still ships them.
opencv-python-headless==4.10.0.84
